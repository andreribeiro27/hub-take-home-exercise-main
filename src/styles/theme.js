/* istanbul ignore file */
import {createTheme} from '@material-ui/core';

// Currently we have the MUI base theme
// For overrides please check https://mui.com/customization/theming/#theme-configuration-variables
const theme = createTheme({});

export default theme;
